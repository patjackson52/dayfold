{
  "name": "dayfold",
  "private": true,
  "version": "0.0.0-M0",
  "description": "dayfold monorepo (Dayfold / Sloopworks) (M0). specs/ = source of truth; code under packages/ + apps/.",
  "workspaces": [
    "packages/*",
    "apps/api"
  ],
  "scripts": {
    "codegen": "node packages/schema/codegen.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
